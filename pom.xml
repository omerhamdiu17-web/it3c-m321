<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Spring Boot als Eltern-POM: liefert die abgestimmten Versionen
         aller Abhängigkeiten, damit wir sie nicht einzeln pflegen müssen. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>ch.benedict.m321</groupId>
    <artifactId>it3c-m321</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>M321 Chat-App</name>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!-- Weitere Dienste kommen hier dazu: load-generator. -->
    <modules>
        <module>chat-service</module>
        <module>web-gateway</module>
        <module>batch-writer</module>
    </modules>

    <!-- Spring Cloud liefert das Gateway (Proxy auf Keycloak). Die Linie
         2025.0.x passt zu Spring Boot 3.5; der Import sorgt dafür, dass
         alle Spring-Cloud-Teile in zueinander passenden Versionen kommen. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>2025.0.3</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
